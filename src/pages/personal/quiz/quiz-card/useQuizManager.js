

const useQuizManager = (deck) => {
    // sort by perf
    // filter out those with 16 >= perf >= 4, consider = 3, 2, 1 till 0   : Just to focus on the same card over and over
    // if there are, we pick first 10 of them.

}

export default useQuizManager